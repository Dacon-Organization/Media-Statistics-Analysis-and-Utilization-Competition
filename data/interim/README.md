# data/interim/ — 중간 가공 데이터

`raw/`에서 정제·병합하는 **중간 단계 산출물**을 둡니다. (예: 연도 통합 long 포맷, 변수명 하모나이즈 결과)

- **버전관리 제외**(`.gitignore`). 노트북/`src/` 파이프라인으로 `raw/`에서 **재생성** 가능해야 합니다.
- 파일명 권장: `<단계>_<설명>.parquet` 등. 재현 스크립트를 함께 남깁니다.
